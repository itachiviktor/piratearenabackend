CREATE TABLE SelectedCharacters (
    id int NOT NULL AUTO_INCREMENT,
    character1 int NOT NULL,
    character2 int NOT NULL,
    character3 int NOT NULL,
    userToken varchar(255) NOT NULL,
    PRIMARY KEY (ID)
);

